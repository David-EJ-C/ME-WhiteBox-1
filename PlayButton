using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

//I include the scenemanagement library from the game engine resources for UI integration and use

public class PlayButton : MonoBehaviour
{

    // i create a public void method to allow the scene in question to be loaded on click with a string value in parathesises
    public void PlayGame()

    {
        //SceneManager.LoadScene("Menu");
        //SceneManager.LoadScene("");
    }

    // i create a public void method to allow the scene in game to be exited unless the player loses.
    public void QuitGame()
    {
        Application.Quit();    
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
